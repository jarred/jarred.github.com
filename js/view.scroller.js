// Generated by CoffeeScript 1.3.3
(function() {
  var jb;

  jb = window.JB || (window.JB = {});

  jb.Views || (jb.Views = {});

  jb.Views.Scroller = Backbone.View.extend({
    initialize: function(o) {
      this.o = o;
      return;
      _.bindAll(this);
      this.$el = $(this.el);
      this.$el.addClass('hide');
      setInterval(this.position, 20);
    },
    position: function() {
      var h, newY, pc, y;
      y = $('body').scrollTop() || 0;
      h = $(document).height() - $(window).height() || 0;
      pc = Math.round((y / h) * 100);
      newY = pc * h;
      this.$el.css({
        top: "" + newY
      });
    }
  });

}).call(this);
